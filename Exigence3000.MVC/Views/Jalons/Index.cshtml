@model IEnumerable<Exigence3000.MVC.Service.Jalon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date_prevue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_livraison)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Projet.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Responsable.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date_prevue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_livraison)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Projet.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Responsable.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
